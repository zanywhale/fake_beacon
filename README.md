# fake_beacon
Make fake beacon frame with libtins


```sh
$ git clone https://github.com/mfontanini/libtins
$ cd libtins
$ git checkout v3.5
$ mkdir build
$ cd build
$ cmake ../
$ make
$ sudo make install
$ sudo ldconfig
```
